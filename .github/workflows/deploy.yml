name: Build and Deploy

on:
  push:
    branches:
      - main
      - dev
      - feat_deploy
  workflow_dispatch:

  
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Build main branch
        run: |
          git fetch origin main
          git checkout main
          echo "Building main branch output..."
          pnpm build --outDir dist

      - name: Build dev branch
        run: |
          git fetch origin dev
          git checkout dev
          echo "Building dev branch output..."
          pnpm build --outDir dist/dev

      - name: Build feat_deploy branch
        run: |
          git fetch origin feat_deploy
          git checkout feat_deploy
          echo "Building feat_deploy branch output..."
          pnpm build --outDir dist/feat_deploy

      - name: Prepare Pages bundle
        run: |
          touch dist/.nojekyll

      - name: Upload build output
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-deploy
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
